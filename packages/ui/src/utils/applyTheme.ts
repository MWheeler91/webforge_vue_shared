// Compatibility export for callers outside the color-theme store. Keep one
// canonical key list, default resolver, and DOM application path.
export { applyThemeColors as applyTheme } from '../config/color.runtime'
