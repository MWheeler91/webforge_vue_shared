import type { AppFooterProps } from './AppFooter.types'

export const appFooterPreview = {
  brand: { label: 'app' },
  links: { keyed: {}, grouped: {}, items: [{ label: 'Overview', href: '#overview' }] },
} satisfies AppFooterProps
