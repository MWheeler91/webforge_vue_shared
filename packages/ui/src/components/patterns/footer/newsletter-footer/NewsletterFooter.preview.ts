import type { NewsletterFooterProps } from './NewsletterFooter.types'

export const newsletterFooterPreview = {
  brand: { label: 'newsletter' },
  links: { keyed: {}, grouped: {}, items: [{ label: 'Overview', href: '#overview' }] },
} satisfies NewsletterFooterProps
