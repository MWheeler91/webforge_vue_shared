import type { SocialFooterProps } from './SocialFooter.types'

export const socialFooterPreview = {
  brand: { label: 'social' },
  links: { keyed: {}, grouped: {}, items: [{ label: 'Overview', href: '#overview' }] },
} satisfies SocialFooterProps
