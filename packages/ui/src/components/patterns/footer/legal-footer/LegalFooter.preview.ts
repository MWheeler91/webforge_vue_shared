import type { LegalFooterProps } from './LegalFooter.types'

export const legalFooterPreview = {
  brand: { label: 'legal' },
  links: { keyed: {}, grouped: {}, items: [{ label: 'Overview', href: '#overview' }] },
} satisfies LegalFooterProps
