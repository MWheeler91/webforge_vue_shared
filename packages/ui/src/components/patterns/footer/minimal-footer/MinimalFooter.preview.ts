import type { MinimalFooterProps } from './MinimalFooter.types'

export const minimalFooterPreview = {
  brand: { label: 'minimal' },
  links: { keyed: {}, grouped: {}, items: [{ label: 'Overview', href: '#overview' }] },
} satisfies MinimalFooterProps
