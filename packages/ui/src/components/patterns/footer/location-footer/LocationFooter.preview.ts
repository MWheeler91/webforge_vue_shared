import type { LocationFooterProps } from './LocationFooter.types'

export const locationFooterPreview = {
  brand: { label: 'location' },
  links: { keyed: {}, grouped: {}, items: [{ label: 'Overview', href: '#overview' }] },
} satisfies LocationFooterProps
