import type { ContactListProps } from './ContactList.types'

export const contactListPreview = {
  heading: { text: 'contact' },
  items: { keyed: {}, grouped: {}, items: [{ label: 'Preview item' }] },
} satisfies ContactListProps
