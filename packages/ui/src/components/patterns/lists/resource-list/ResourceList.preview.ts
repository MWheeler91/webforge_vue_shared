import type { ResourceListProps } from './ResourceList.types'

export const resourceListPreview = {
  heading: { text: 'resource' },
  items: { keyed: {}, grouped: {}, items: [{ label: 'Preview item' }] },
} satisfies ResourceListProps
