import type { FeatureListProps } from './FeatureList.types'

export const featureListPreview = {
  heading: { text: 'feature' },
  items: { keyed: {}, grouped: {}, items: [{ label: 'Preview item' }] },
} satisfies FeatureListProps
