import type { LinkListProps } from './LinkList.types'

export const linkListPreview = {
  heading: { text: 'link' },
  items: { keyed: {}, grouped: {}, items: [{ label: 'Preview item' }] },
} satisfies LinkListProps
