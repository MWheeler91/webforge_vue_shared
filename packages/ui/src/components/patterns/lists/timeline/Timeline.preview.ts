import type { TimelineProps } from './Timeline.types'

export const timelinePreview = {
  heading: { text: 'timeline' },
  items: { keyed: {}, grouped: {}, items: [{ label: 'Preview item' }] },
} satisfies TimelineProps
