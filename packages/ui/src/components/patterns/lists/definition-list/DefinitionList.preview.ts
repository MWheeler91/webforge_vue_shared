import type { DefinitionListProps } from './DefinitionList.types'

export const definitionListPreview = {
  heading: { text: 'definition' },
  items: { keyed: {}, grouped: {}, items: [{ label: 'Preview item' }] },
} satisfies DefinitionListProps
