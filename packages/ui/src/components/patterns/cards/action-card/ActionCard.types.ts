import type { SharedCardProps } from '../../../primitives/card/card.types.ts'

/**
 * DRAFT — not part of the public contract. See the note in `../index.ts`
 * before changing or exporting this type.
 */
export type ActionCardProps = SharedCardProps
