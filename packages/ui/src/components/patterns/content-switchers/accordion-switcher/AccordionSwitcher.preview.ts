import type { AccordionSwitcherProps } from './AccordionSwitcher.types'

export const accordionSwitcherPreview = {
  heading: { text: 'accordion' },
  items: { keyed: {}, grouped: {}, items: [{ id: 'one', label: 'One', heading: { text: 'One' } }] },
} satisfies AccordionSwitcherProps
