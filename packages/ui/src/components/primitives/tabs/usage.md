# Tabs

Compose `BaseTabs`, `TabList`, `TabTrigger`, and `TabPanel` using matching values. Use `v-model` for controlled state or `defaultValue` for internal state. Arrow keys move selection and focus.
